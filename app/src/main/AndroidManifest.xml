<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HarmoniaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting"
        tools:targetApi="31">
        <activity
            android:name=".presentation.home.HomeActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.Harmonia" />
        <activity
            android:name=".presentation.home.detail.DetailActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.Harmonia" />
        <activity
            android:name=".presentation.analyze.AnalyzeActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.Harmonia" />
        <activity
            android:name=".presentation.analyze.result.ResultActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.Harmonia" />

        <activity
            android:name=".presentation.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>